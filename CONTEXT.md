# Rihla-frontend (Ahmed) — Handoff

> Read relevant section only. Appends 3–6 lines. Prune Changelog > 25.
> Last updated: 2026-08-08

## Current status
- Fully built from remote `7fc2fd5` (wiring-productionization merged). `npm ci` done; `npm run build` clean (24 routes, tsc green).
- Local dev runs on :3001 with same-origin `/api` rewrite → `CORE_API_URL=http://localhost:3000` (local Core; **Core is the only gateway the client talks to** — no direct GeoContext :8000 / ai :3003 / Risk calls; external 3rd parties: OSRM/Nominatim/Google Maps/Unsplash only). Restart dev server to pick up `.env.local` changes. Note: the VPS Core (:3005) has a DIFFERENT `JWT_ACCESS_SECRET`, so a browser token minted against the VPS is 401 on local Core — sign in again after switching.

## In-progress / next
- 2026-08-12 GEO POIs LIVE: Explore now renders real GeoContext POIs in the radius area — `CORE_API_URL=http://localhost:3000` (local Core, `tsx watch`) → `GIS_SERVICE_URL=http://localhost:8000`. Browser-verified at 5 km: "468 places on map" w/ real sites (Egyptian Museum, Al-Sheikh Maaruf Mosque) near Cairo; teleport (LocationMocker) → Luxor "100 places on map" (Luxor Temple, Mummification Museum); 0 console errors.
- Location: device GPS unavailable in headless test browser → shows "Location unavailable" until set. LocationMocker teleport (bottom-right, dev-only) and map-click pin both work; emergency page reflects override.
- Next: deploy Core geo.category fix (omit all-heritage comma-join) from this session to VPS; restore `CORE_API_URL=http://88.222.220.235:3005` only after VPS GeoContext is running.

## Purpose / non-goals
- This is the design-accurate prototype; the functional app lives in `/rihla-client`.
- Wiring in progress is tracked in `WIRING_GUIDE.md` (screen→endpoint matrix + setup order).
- Do NOT let this diverge from the monorepo as source of truth for payload shapes.

## In-progress / next
- (Resume point) Wiring follows the order in WIRING_GUIDE §5: (1) `.env` + axios base → (2) auth store + `GET /users/me` → (3) Rafiq chat → (4) Explore/History/Wallet/Profile → (5) Safety/Currency + static emergency.

## Key files
- `src/app/App.tsx` — the static screen router (source of all hardcoded data)
- `src/lib/api/` — client/config/token-manager/types (already built; reuse)
- `WIRING_GUIDE.md`, `RESTYLE_SPEC.md`, `ATTRIBUTIONS.md`
- `/egymonuments.clean.json`, `/Emergency_Contacts.json` → intended as static/public data.

## Gotchas
- Endpoint gesture: Core base `http://localhost:3000/api`; Geo `http://localhost:8000/api/v1`.
- Google OAuth not implemented — hide "Continue with Google" (WIRING_GUIDE §3.2).
- Rafiq persona + i18n (`[locale]`/next-intl) are client-side; no backend field.

## Changelog
- 2026-08-19: PULLED origin →f8afb0f (`feat: link image identification to active conversation` — chatService.identify now posts `conversation_id` to tie image IDs into the active conversation; RafiqModal/rafiq page pass `conversationId`) and PUSHED to fork `ibrahim99035/Rihla-frontend` (Vercel watches fork). Verified live bundle: `conversation_id` present in `/app/rafiq` chunk `page-94049c03c1f5748d.js`; login via `https://rihla-frontend.vercel.app/api/auth/login` → 200 admin, proxy → VPS :3005 OK.
- 2026-08-14: DEPLOYED TO VERCELL. Committed all UI polish (mobile header declutter + sidebar-over-all z-index + SSE zone notice + SensitiveAreaNotice + unique site ids + custom scrollbars) as `a7eba05` and pushed to BOTH `origin` (Rihla-Ecosystem/Rihla-frontend) AND the personal fork `ibrahim99035/Rihla-frontend` — ⚠ Vercel watches the FORK, not the org repo (org was 5 commits ahead of fork after a rebase; force-pushed fork to match org). Verified live bundle contains new markers (`rihla-top-loc`, `Expand sidebar`, `sidebar-collapsed`, `toggleCollapsed`); site title OK, `/api/*` rewrite → VPS :3005 works. NOTE: first push to org was rejected (origin had a merge PR from the fork); resolved via clean rebase.
- 2026-08-14: MOBILE HEADER + SIDEBAR-OVER-ALL FIX. Two issues on mobile: (1) TopBar crammed brand (210px) + location chip (148px) + bell + avatar into 390px — the brand button (x:64–274) fully overlapped the location chip (x:126–274). Added responsive CSS in TopBar `<style>` block (`@media max-width:640px`): hide location chip (`.rihla-top-loc`), hide brand subtitle (`.rihla-top-brand-sub`), scale glyph 0.72 (`.rihla-top-brand-glyph`), hide divider. Mobile header now = hamburger + compact brand + bell + avatar, no overlap, no overflow. Desktop unchanged. (2) Mobile sidebar drawer sat at zIndex:45 while page content reaches 1400 and toasts 9999 — so the drawer got covered by explore banner/cards. Raised backdrop to zIndex 9998 and drawer to 10000 (sidebar-over-all RULE) in AppShell.tsx. Verified: drawer on top of banner(980)/cards(950)/modals(1200-1400)/toasts(9999), opens/closes, desktop intact, `tsc` clean, 0 console errors.
- 2026-08-14: FULL UI HEALTH CHECK — PASS. Swept all 9 app pages (home, explore, monuments, quests, safety, saved, wallet, profile, settings) at desktop 1366 + mobile 390. Results: 0 console errors on every page, 0 horizontal overflow/stray elements (the single off-canvas div on home is the intentional rotating solar-disc decoration, clipped by `overflow:hidden`), content present everywhere, Leaflet map renders 20/20 tiles + 194 markers. Header toggles verified: desktop collapse 220→64, mobile hamburger opens drawer, nav-item click navigates + closes drawer, backdrop tap closes drawer. Typecheck clean. Known pre-existing (not introduced by this change): home page has no mobile hamburger (its custom header never had one) — mobile users on home navigate via the dashboard service cards, not the drawer.
- 2026-08-14: HEADER RESPONSIVE VISIBILITY FIX. The mobile hamburger (`lg:hidden`) and header collapse button (`hidden lg:flex`) both set `display:'flex'` inline, which overrode the Tailwind responsive classes — so on desktop the hamburger showed alongside the collapse chevron, and on mobile the collapse chevron showed next to the hamburger. Removed `display` from both inline styles; hamburger now `className="flex lg:hidden"`, collapse button `className="hidden lg:flex"`. Verified desktop (hamburger hidden, chevron visible) + mobile 390px (chevron hidden, hamburger at x:14, brand + bell only, no overflow), 0 console errors, `tsc` clean.
- 2026-08-14: EXPLORE DUPLICATE-KEY FIX. React error "two children with same key `2`" in ExplorePage: GeoContext's merged multi-category POI response carries colliding numeric ids (each category query yields its own 1,2,3…), so `key={site.id}` on the recommended-sites buttons (page.tsx) duplicated. Added `ensureUniqueSiteIds()` in `explore/page.tsx` — renumbers colliding ids to the next free value — applied to all `setSites()` paths (live POIs + fallback). Verified 0 console errors over 8s settle; `tsc` clean.
- 2026-08-14: UI POLISH — banner z-index, custom scrollbar, collapsible sidebar. (1) `SensitiveAreaNotice.tsx` banner z-index 900→980 so it sits above the Explore "places on map" card (950) but below modals (1000/1200). (2) `globals.css` custom scrollbar (webkit + `scrollbar-color/width`): nile-tinted track, copper→terracotta gradient thumb, 8px, rounded. (3) `AppShell.tsx` sidebar collapse: clearer ChevronLeft/Right toggle (was subtle Menu button), persisted to localStorage `rihla:sidebar-collapsed` (restored in `useEffect` to avoid SSR hydration mismatch — first version read localStorage in `useState` init and threw a hydration error). 220px↔64px, verified across reload. All browser-verified 0 console errors; `tsc` clean.
- 2026-08-14: ZONE LAW POPUP FIX + SSE 401 FIX. (1) Explore map polygon click opened no modal: `InteractiveMap.tsx` bound the click to the 2px dashed outline only and made the fill non-interactive, so center clicks hit the tiles. Fill layer is now interactive and carries the click; outline is decorative. Verified live at 30.05158,31.31103: click → `ZoneLawPopup` opens with RAG rules (Law 216/2017 drone ban, photography) + citations. (2) `zoneNoticeService.ts` SSE stream hit 401s on token expiry and retried the stale token in a backoff loop (console spam, no recovery until an axios call refreshed). Now on 401 it calls `refreshAccessToken()` (single-flight, shared with axios interceptor) and retries immediately with the fresh token. Verified by corrupting the stored token: stream shows ONE 401 then 200, token auto-restored, 0 console errors during 20s stream settle. `tsc` clean. NOTE: `JWT_ACCESS_EXPIRY=15m` locally — expired tokens still produce a single transient 401 burst at refresh boundary; that is normal axios refresh behavior.
- 2026-08-14: SENSITIVE-AREA / ANONYMOUS ZONE WARNINGS WIRED (local E2E verified). Explore banner (SSE enter/exit + `/geo/notice` bootstrap), law modal from banner + map polygon, red/amber/teal polygons; zone identity kept private in all payloads.
- 2026-08-12: EXPLORE GEO POIs FIXED. Root cause: Explore's radius area showed only the 92 ticketed catalog because `/api/geo/pois` returned `{"pois":null}` — GeoContext was DOWN on VPS + local (Core `.catch(()=>null)`). Fixes: Core `geo.service.ts` no longer forwards the comma-joined heritage `categories` as GeoContext's single `category` (omits filter for the all-heritage default, forwards single category otherwise) — `max:5000` radius clamp lifted in GeoContext `config.py` (→1,000,000 m); restarted `geocontext_db` (5433) + GeoContext on :8000; `.env.local` `CORE_API_URL=http://localhost:3000`. Verified: Explore "468 places on map" w/ real POIs near Cairo, Luxor teleport → "100 places", emergency page location override, 0 console errors, `tsc` clean. NOTE: VPS token ≠ local Core secret → 401; re-sign-in.
- 2026-08-12: TTS SPEAKER ICON FIX: speaker icons (emergency scenario guide "Read"/"Voice guide"/per-step, Arabic phrase cards "Hear …") never made a sound. Cause in `src/lib/speech.ts`: `speak()` checked `voicesFor().length > 0` synchronously — Chrome loads voices async, so cold/first click saw empty list and wrongly fell back to Google Translate `translate_tts`, which Chrome now blocks (`net::ERR_BLOCKED_BY_ORB`, verified in browser network log). Fix: when `speechSynthesis` exists, always use it (it picks a voice lazily via `voiceschanged`); Google TTS kept only for browsers with no speech API. Verified: click fires zero network requests, no console errors, `tsc` clean. NOTE: headless test browser has no TTS voices (`voices=0`) so no audible sound there — real browser should now read aloud.
- 2026-08-12: EMERGENCY PAGE BOTTOM WHITE FIX: `/app/safety/emergency` showed cream (`#F5EFE0` body) below the fold — the page root had `flex: 1` inside AppShell `<main>` (`overflowY:auto`, viewport-height), so its `#0D0B09` background stopped at viewport (root 681px) while content columns ran to ~1111px. Changed root to `flex: '0 0 auto'` + `width:100%` + `minHeight:'100%'` → root now grows with content (offsetHeight 1155 == main scrollHeight == deepest grid bottom). Browser-verified 0 new console errors (only pre-existing `/placeholder.svg` 404); `tsc --noEmit` clean.
- 2026-08-12: PHASE 3–5: emergency + reports + dev widgets. `EmergencyContacts` now real-dials via `tel:` (rows + SOS→126) and SOS auto-submits a CRITICAL SAFETY report via `incidentReportService` (lat/lng from `LocationProvider`). New shared `ReportIssueModal` (type/severity/description/≥10 chars, coords + site, success screen, `showToast`) with entry buttons on safety page ("Report a travel issue" next to Active Alerts) + emergency page ("Report a non-emergency issue"). `incidentReportService` REWRITTEN to axios (`apiClient.post`) — old `apiClient.POST` openapi-style call didn't exist on the axios client → `apiClient.POST is not a function`. Browser-verified: safety modal submit → success screen → report persisted in Core (`GET /api/reports`: SCAM/HIGH/PENDING). Dev widgets repositioned off the left sidebar: `LocationMocker` → `fixed right:88 bottom:24`, `TestHub` → `fixed right:24 bottom:96` (clears safety SOS at right:24/bottom:24). `tsc` clean; dev server restart needed after an interrupted `next build` wrecked `.next` (build skipped; live dev smoke 0 errors, only pre-existing `/placeholder.svg` 404).
- 2026-08-12: RAFIQ MODAL (Phase 2). New `components/rafiq/RafiqProvider.tsx` (context `useRafiq` exposing `openRafiq({context?, initialQuery?})`, hosts the modal) mounted in `app/app/layout.tsx` inside `ErrorBoundary` around `AppShell`. Rewired every bot callout from `router.push('/app/rafiq')` to the in-page modal (route stays live): `AskRafiqButton` (profile/quests/quests-[slug]/safety), site detail top button + `SiteRightSidebar` `setRafiq`, explore `handleRafiq` + TopBar, monuments list/detail, currency/saved/wallet/quests/safety TopBars, emergency `EmergencyRightColumn` `setRafiq` (uses `buildExploreContext`/`buildSafetyContext`/`buildJourneyContext` + `initialQuery` for alert headlines). Browser-verified: sites/1 modal opens with contextual welcome + topic pills + live LLM streaming (markdown), quests modal shows journey-step context, close works; 0 console errors. `tsc` clean. `/app/rafiq` standalone unaffected.
- 2026-08-12: FAVORITES WIRED. `placesApi.listFavorites/isFavorited/addFavorite/removeFavorite` now unwrap Core's `{success,data}` wrapper (they previously expected bare arrays → saved page always empty). Backs a NEW Core feature (`/api/places/favorites` CRUD + `/places/events` ack) built this session; Core NOT yet deployed so dev proxy currently points at `http://localhost:3000`. Browser smoke passed: Save→Saved page (image/story/price)→Remove→empty, 0 console errors (the old `/places/events` 404 is gone). Also cleaned a stale `adminOnly` destructure in `AppShell.tsx` after the admin page was removed.
- 2026-08-12: ADMIN FIX: `/app/admin` crashed with `users.filter is not a function` — Core returns `{users,pagination}` for `/admin/users` and `{logs}` for `/admin/audit-logs` (not bare arrays). `adminService.getUsers/getAuditLogs` now unwrap both shapes; page renders 14 users, audit trail, token packages cleanly (0 console errors). Core OpenAPI annotation for these still wrongly says `type: array` (cosmetic; frontend defends both).
- 2026-08-12: UI SMOKE (browser, chromium via fixed browser MCP `--executable-path`): unauthed `/app` → `/login?next=%2Fapp`; login→`/app` grounded; home live 32°C/UV/AQI; Rafiq streamed a full answer then hit backend 500 → graceful offline-guidebook fallback (backend Gemini `AI temporarily unavailable`, 3/3 curl) = correct behavior; wallet/safety/profile/admin all live with 0 console errors. Setup: `.env.local` (`NEXT_PUBLIC_API_BASE_URL=/api`) + `next.config.ts` `rewrites()` `/api/:path*`→VPS (mirror of vercel.json), `CORE_API_URL` env overridable.
- 2026-08-12: OPENAPI REGEN: reproduced Core `swaggerSpec` locally (73 paths vs stale 62) → `openapi-typescript` regenerated `src/api/generated/{openapi.json,types.ts}`. Core spec had broken `$ref` to undefined `Role` schema → added `Role` to `swagger.ts` components; Core `tsc` clean. Frontend build passes with the authoritative types.
- 2026-08-12: BUGFIXES: `chatAdapter` now reads Core's `{ response, ... }` (was `response.text`) with conversationId in meta; `/app` layout redirects unauthenticated users to `/login?next=<path>`; login honors `next` (was always `/app`).
- 2026-08-08: Rewrote `WIRING_CORRECTIONS.md` — env blocks now embedded BYTE-EXACT via diff-verification against source files (Core/ai/Geo/Risk env + `.env.cloud` + rihla-prod/env + client `.env.local`). Corrected §2 (one gateway, `/safety/*` not `/risk/*`, refresh-cookie path preservation, hide Google OAuth, password rules) + §3 plan (local-with-cloud, Vercel rewrite). ⚠ Contains live secrets; never commit/push it.
- 2026-08-07: Created `AGENTS.md` + `CONTEXT.md` (from WIRING_GUIDE + package.json). Not run/wired yet.- 2026-08-13: VERCEL CORS + DEPLOY FIX. Tested https://rihla-frontend.vercel.app (loads, title OK) — console showed 2 errors: favicon 404 (cosmetic) + browser calling http://localhost:3000/api/auth/refresh (NEXT_PUBLIC_API_BASE_URL unset in Vercel build → fell back to default). Added https://rihla-frontend.vercel.app to CORS_ORIGIN in local Core .env + VPS /opt/rihla-prod/env, recreated rihla-core (docker-compose v1). Preflight OPTIONS → 204 verified. Created `.env.production` (NEXT_PUBLIC_API_BASE_URL=/api) so Vercel build uses same-origin /api → vercel.json rewrite → VPS Core :3005. Needs Vercel redeploy to take effect.
- 2026-08-13: FAVICON from logo. Generated public/favicon.ico (64px), apple-touch-icon.png (180), icon-192/512.png (center-crop of 1536x1024 logo.png) + manifest.webmanifest; metadata in src/app/layout.tsx now has icons + manifest (fixes the Vercel favicon.ico 404). tsc clean.
- 2026-08-13: FEATURE SWEEP (browser, local dev stack on 3001/3000/3003/8000/3004). Every main feature verified live with 0 console errors: Home (live weather 35°C/UV/AQI), Explore (476 places), Monuments (92 · 17 govs), place Detail + Save→Saved (Giza plateau LE700 + Rawda Island Nilometer LE120 persisted, Saved shows 2 places/2 govs), Currency (live rates USD/EUR/GBP/SAR/AED, converter, banknote catalog), Wallet (0-token balance + Paymob packages render), Quests (Scam Shield Completed 5/5 +50XP, others Not started), Profile (Admin User L2·135XP), History (empty state OK), Settings (persona/lang/location toggle; POST /api/memory/preferences → 200 persisted), Safety (live advisory), Emergency SOS (click → "SOS logged & calling 126"; CRITICAL SAFETY report e80dff2a with lat/lng persisted in Core /api/reports), Rafiq modal (full markdown streaming). Fixed leftover /placeholder.svg 404 → added public/placeholder.svg (map-unavailable backdrop). Only remaining console noise: harmless logo width/height aspect warning. Vercel redeploy still pending for .env.production + favicon + placeholder.
- 2026-08-13: VERCEL REDEPLOY APPROVED (Step 1-3 done). Root cause of broken live app: deployed bundle was built without NEXT_PUBLIC_API_BASE_URL → browser fell back to http://localhost:3000 (config.ts:7) → 401s. Fix: committed .env.production (NEXT_PUBLIC_API_BASE_URL=/api) + favicon/manifest/placeholder assets as ac2e275, pushed to the PERSONAL fork remote `fork` = ibrahim99035/Rihla-frontend.git (origin stays org Rihla-Ecosystem — untouched, verified origin/main=3cbbef2f). Vercel redeployed from GitHub. Verified live: 0 localhost in any chunk, favicon/manifest 200, login 200 → /app, all API same-origin /api → VPS :3005 (env 200 w/ live 31°C/AQI 2, memory/preferences 200). Only console error = pre-login refresh 401 (expected, no cookie yet). VPS account shows Level 1 · 0 XP (different DB from local dev). Screenshot: vercel-approved-home.png.
- 2026-08-13: PWA + APK + iOS polish shipped on fork (4 commits aa2f3a3..4bd82ab, org untouched): sw.js + RegisterSW + sw headers (installable), maskable 512 icon, appleWebApp meta + manifest id/lang. Verified local prod build + live Vercel (sw 200, manifest maskable, iOS meta in head). Android APK via PWABuilder TWA from live URL (no repo change). iOS = PWA Add-to-Home-Screen strategy (no .ipa; needs Mac + $99/yr if App Store ever wanted).
- 2026-08-14: SENSITIVE-AREA SYSTEM (server-push). New `src/services/zoneNoticeService.ts` (fetch-based SSE `subscribeZoneStream` w/ backoff + auth bearer + `reportLocation` throttle, mirroring chatService); `src/app/components/safety/SensitiveAreaNotice.tsx` fixed top banner (severity-styled, dismiss w/ latch, `useLocation`, bootstraps via `/geo/notice` + live via SSE `zone` enter/exit, "View laws & guidelines" → RAG/AI popup); mounted in `app/app/layout.tsx` inside RafiqProvider. `InteractiveMap.tsx` new `zonePolygons`/`onZoneClick` → red/amber/teal dashed-outline anonymous polygons (click → popup). Explore page fetches `getZonePolygons(origin,1000)` + `handleZoneClick` → `ZoneLawPopup`. Zone identity never exposed. `tsc --noEmit` clean. Backend (Core `/geo/zones` `/geo/law` `trackZoneState` SSE `publishContextEvent`, GeoContext anon `/api/v1/context/zones`, ai-service `/legal`) built but NOT yet deployed to VPS; local Core must be rebuilt/restarted.
